// Reemplaza los valores con la configuración de tu proyecto Firebase.
// Puedes obtener estos valores desde el panel de Firebase > Configuración del proyecto > Tus apps (Web).
export const firebaseConfig = {
  apiKey: "REPLACE_API_KEY",
  authDomain: "REPLACE_AUTH_DOMAIN",
  projectId: "REPLACE_PROJECT_ID",
  storageBucket: "REPLACE_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_MESSAGING_SENDER_ID",
  appId: "REPLACE_APP_ID"
};

// Nota: Guarda este archivo y luego sustituye los valores por los de tu proyecto.
